#include <bits/stdc++.h>
using namespace std;
#define llu long long int
#define FOR(i,a,b) for (int i=(int)(a);i<(int)(b);i++)
int main() { 
	int x = 0, curr;
	cin >> curr;
	if (x < curr) x = curr;
	cin >> curr;
	if (x < curr) x = curr;
	cin >> curr;
	if (x < curr) x = curr;
	cin >> curr;
	if (x < curr) x = curr;
	cin >> curr;
	if (x < curr) x = curr;
	cin >> curr;
	if (x < curr) x = curr;
	cin >> curr;
	if (x < curr) x = curr;
	cin >> curr;
	if (x < curr) x = curr;
	cin >> curr;
	if (x < curr) x = curr;
	cin >> curr;
	if (x < curr) x = curr;
	cout << x;
	return 0;
}
